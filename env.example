# Notion API Configuration
NOTION_API_KEY=your_notion_api_key_here
NOTION_DATABASE_ID=your_notion_database_id_here

# Netlify Blobs Configuration (for file uploads)
# Functions API v2 should provide automatic Blobs context
# Only add manually if automatic detection fails:
# NETLIFY_SITE_ID=your_site_id_here (auto-extracted from URL)
# NETLIFY_TOKEN=your_netlify_personal_access_token_here (generate from User Settings > Applications > Personal access tokens)

# SMTP Configuration for Email Notifications (Gmail)
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here
